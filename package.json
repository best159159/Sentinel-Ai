{
    "name": "sentinel-ai-vibehack",
    "version": "1.0.0",
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
        "install:all": "npm install --legacy-peer-deps && npm install --prefix backend --legacy-peer-deps && npm install --prefix frontend --legacy-peer-deps",
        "build:all": "npm run install:all && cd frontend && npm run build",
        "start": "cd backend && node server.js"
    },
    "dependencies": {
        "concurrently": "^8.2.2"
    }
}