{
    "name": "sentinel-ai-vibehack",
    "version": "1.0.0",
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
        "install:all": "npm install --legacy-peer-deps && npm install --prefix backend --legacy-peer-deps && npm install --prefix frontend --legacy-peer-deps",
        "build:all": "cd frontend && npm run build",
        "start": "concurrently \"BACKEND_PORT=5001 node backend/server.js\" \"PORT=${PORT:-3000} node frontend/.next/standalone/server.js\""
    },
    "dependencies": {
        "@dataconnect/generated": "file:src/dataconnect-generated",
        "concurrently": "^8.2.2"
    },
    "overrides": {
        "react": "18.3.1",
        "react-dom": "18.3.1"
    }
}
